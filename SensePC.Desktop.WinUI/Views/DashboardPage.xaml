<Page
    x:Class="SensePC.Desktop.WinUI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SensePC.Desktop.WinUI.Views"
    xmlns:controls="using:SensePC.Desktop.WinUI.Controls"
    xmlns:vm="using:SensePC.Desktop.WinUI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SenseBackgroundBrush}" Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Spacing="8" Margin="0,0,0,32">
            <TextBlock Text="Good Morning," Foreground="Gray" FontSize="16"/>
            <TextBlock Text="Workspace" Foreground="White" FontSize="28" FontWeight="Bold"/>
        </StackPanel>

        <GridView Grid.Row="1" ItemsSource="{x:Bind ViewModel.PCs}" SelectionMode="None">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="vm:PCInstance">
                    <controls:PCInstanceCard ViewModel="{x:Bind}" Margin="0,0,16,16" />
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
