<Page
    x:Class="SensePC.Desktop.WinUI.Views.TutorialsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Video Tutorials" 
                       Style="{StaticResource TitleTextBlockStyle}"
                       Margin="0,0,0,4"/>
            <TextBlock Text="Learn how to make the most of your Sense PC" 
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Main Content Card -->
        <Border Grid.Row="1" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Filters Bar -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Padding="16">
                    <AutoSuggestBox x:Name="SearchBox"
                                    PlaceholderText="Search tutorials..."
                                    QueryIcon="Find"
                                    Width="280"
                                    TextChanged="SearchBox_TextChanged"/>
                    
                    <ComboBox x:Name="CategoryFilter"
                              PlaceholderText="Category"
                              Width="140"
                              SelectionChanged="CategoryFilter_SelectionChanged"/>
                    
                    <ComboBox x:Name="DifficultyFilter"
                              PlaceholderText="Difficulty"
                              Width="140"
                              SelectionChanged="DifficultyFilter_SelectionChanged"/>
                </StackPanel>

                <!-- Tutorials List -->
                <GridView x:Name="TutorialsGridView" 
                          Grid.Row="1"
                          Padding="16,8,16,16"
                          SelectionMode="None"
                          IsItemClickEnabled="True"
                          ItemClick="TutorialsGridView_ItemClick">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="320" Height="240" Margin="4">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="140"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Thumbnail -->
                                <Grid Grid.Row="0" CornerRadius="4,4,0,0">
                                    <Image Source="{Binding ThumbnailUrl}" Stretch="UniformToFill"/>
                                    <Border Background="#80000000" CornerRadius="4,4,0,0"/>
                                    
                                    <!-- Play Icon -->
                                    <FontIcon Glyph="&#xE768;" FontSize="40" Foreground="White"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    
                                    <!-- Duration Badge -->
                                    <Border Background="{ThemeResource AccentFillColorDefaultBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Margin="8">
                                        <TextBlock Text="{Binding Duration}" 
                                                   Foreground="White" 
                                                   FontSize="12"/>
                                    </Border>
                                    
                                    <!-- Category Badge -->
                                    <Border Background="{ThemeResource SystemFillColorSuccessBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Top"
                                            Margin="8">
                                        <TextBlock Text="{Binding Category}" 
                                                   Foreground="White"
                                                   FontSize="12"/>
                                    </Border>
                                </Grid>
                                
                                <!-- Content -->
                                <StackPanel Grid.Row="1" Padding="12">
                                    <TextBlock Text="{Binding Title}" 
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Description}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               MaxLines="2"
                                               TextTrimming="CharacterEllipsis"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                    
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="Margin" Value="4"/>
                            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}"/>
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </GridView.ItemContainerStyle>
                </GridView>

                <!-- Empty State -->
                <StackPanel x:Name="EmptyState"
                            Grid.Row="1"
                            Visibility="Collapsed"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="12">
                    <FontIcon Glyph="&#xE714;" FontSize="48" 
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="No tutorials found" 
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Try adjusting your search or filters" 
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
