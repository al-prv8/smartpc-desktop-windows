<UserControl
    x:Class="SensePC.Desktop.WinUI.Controls.PCInstanceCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SensePC.Desktop.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SensePC.Desktop.WinUI.ViewModels"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SenseSurfaceBrush}" CornerRadius="8" Padding="16" Width="300" Height="180">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header: Status + Name -->
        <StackPanel Grid.Row="0" Spacing="8">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Ellipse Width="8" Height="8" Fill="#4ADE80" /> <!-- Mock Status Color -->
                <TextBlock Text="{x:Bind ViewModel.Status, Mode=OneWay}" 
                           Foreground="{ThemeResource SensePrimaryBrush}" 
                           FontSize="12" />
            </StackPanel>
            <TextBlock Text="{x:Bind ViewModel.Name, Mode=OneWay}" 
                       Style="{ThemeResource TitleTextBlockStyle}" 
                       Foreground="White" />
        </StackPanel>

        <!-- Specs -->
        <TextBlock Grid.Row="1" 
                   Text="{x:Bind ViewModel.Specs, Mode=OneWay}" 
                   Foreground="Gray" 
                   VerticalAlignment="Center" />

        <!-- Actions -->
        <Button Grid.Row="2" 
                Content="Connect" 
                HorizontalAlignment="Stretch"
                Background="{ThemeResource SensePrimaryBrush}" 
                Foreground="White" />
    </Grid>
</UserControl>
